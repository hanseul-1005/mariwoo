<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mariwoo"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".basic.activity.RelationAddActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Translucent" />
        <activity
            android:name=".basic.activity.DatePickerActivity"
            android:exported="false" />
        <activity
            android:name=".basic.activity.MedicineModifyActivity"
            android:exported="false" />
        <activity
            android:name=".basic.activity.MedicineAddActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Translucent" />
        <activity
            android:name=".basic.MainActivity"
            android:exported="false"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Mariwoo.NoActionBar" />
        <activity
            android:name=".basic.NaverLoginActivity"
            android:exported="false" />
        <activity
            android:name=".basic.KakaoLoginActivity"
            android:exported="false" />
        <activity
            android:name=".basic.user.SignActivity"
            android:exported="false" />
        <activity
            android:name=".basic.TosActivity"
            android:exported="false" />
        <activity
            android:name=".basic.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".basic.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 알람 수신 BroadcastReceiver 등록 -->
        <receiver
            android:name=".basic.receiver.AlarmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>